{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities required by the local project manager window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
